'use client'

import { createTheme } from '@mantine/core'

export const theme = createTheme({
  fontFamily:
    'var(--font-noto-sans-sc), var(--font-noto-sans-jp), var(--font-noto-sans), Helvetica, sans-serif',
})
